import { cleartripSubmit } from '../src/client/js/application'

describe('Testing cleartripSubmit functionality', () => {
    test('cleartripSubmit should be defined', () => {
        expect(cleartripSubmit).toBeDefined()
    })
})
